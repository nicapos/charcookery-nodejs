import { Router } from "express";

const router = Router();

/*
    Define routes here
    ex. router.get('/', controller.getHomePage);
*/

/*
    TODO: Add user routes
    GET /user/ - get user details
    POST /user/ - create user account
    PATCH /user/ - change user password
    DELETE /user/ - delete user account
*/

/*
    TODO: Add recipe routes
    GET /recipes/ - get all community recipes (w/ filters)
    GET /recipes/:userid/ - get all recipes by user (w/ filters)
    GET /recipe/:recipeid/ - get recipe by id
    POST /recipe/ - create recipe
    PATCH /recipe/ - update recipe's favorite status
    DELETE /recipe/ - delete recipe
*/

export default router;
